<Window x:Class="WpfApp2.SongEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Song"
        Height="465"
        Width="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Icon="/Resources/vinyl.ico">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="0">
            <Border Width="126" Height="126" Background="#557A7A7A" ClipToBounds="True">
                <Image Source="{Binding CoverPath}" Stretch="UniformToFill"/>
            </Border>
            <Button Margin="0,10,0,0" Height="32" Content="Browse..." Command="{Binding BrowseCoverCommand}"/>
        </StackPanel>

        <Grid Grid.Row="0" Grid.RowSpan="2" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Title" FontSize="14" Margin="0,0,10,12"/>
            <TextBox Grid.Row="0" Grid.Column="1" Height="32" Margin="0,0,0,12" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Artist" FontSize="14" Margin="0,0,10,12"/>
            <TextBox Grid.Row="1" Grid.Column="1" Height="32" Margin="0,0,0,12" Text="{Binding Artist, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Album" FontSize="14" Margin="0,0,10,12"/>
            <TextBox Grid.Row="2" Grid.Column="1" Height="32" Margin="0,0,0,12" Text="{Binding Album, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Text="Genre" FontSize="14" Margin="0,0,10,12"/>
            <TextBox Grid.Row="3" Grid.Column="1" Height="32" Margin="0,0,0,12" Text="{Binding Genre, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="Year" FontSize="14" Margin="0,0,10,12"/>
            <TextBox Grid.Row="4" Grid.Column="1" Height="32" Margin="0,0,0,12" Text="{Binding Year, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Width="100" Height="36" Background="{StaticResource AccB}" BorderThickness="0" Foreground="White" Content="Save" Command="{Binding SaveCommand}"/>
            <Button Width="100" Height="36" Margin="10,0,0,0" Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
