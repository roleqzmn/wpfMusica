<UserControl x:Class="WpfApp2.Views.PlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10,10,10,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Width="120" Height="120" CornerRadius="4" ClipToBounds="True" Background="#557A7A7A">
                <Image Source="{Binding CoverPath}" Stretch="UniformToFill"/>
            </Border>

            <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Name}" FontSize="44" FontWeight="Bold" Foreground="#1F3E5E"/>
                <TextBlock Text="{Binding SongCountText}" FontSize="28" Foreground="#60798C" Margin="0,2,0,10"/>
                <Button Content="? Play Playlist"
                        Command="{Binding PlayPlaylistCommand}"
                        Background="{StaticResource AccB}"
                        Foreground="White"
                        BorderThickness="0"
                        Width="180"
                        Height="36"
                        HorizontalAlignment="Left"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2"
                  IsReadOnly="True"
                  SelectionUnit="FullRow"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Songs}"
                  SelectedItem="{Binding SelectedSong, Mode=TwoWay}"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="52" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="42" Height="42" CornerRadius="4" ClipToBounds="True" Background="#557A7A7A" Margin="2">
                                <Image Source="{Binding CoverPath}" Stretch="UniformToFill"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="*"/>
                <DataGridTextColumn Header="Album" Binding="{Binding Album}" Width="*"/>
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="*"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration, StringFormat={}{0:m\\:ss}}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
